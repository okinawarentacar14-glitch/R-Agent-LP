# Gitデプロイ作業手順

このドキュメントは、ローカルで行ったファイルの変更を GitHub にプッシュし、本番環境（Cloudflare Pages）に自動反映させるための手順書です。

---

## 🛠️ 基本的な手順の流れ

### 1. 現在の変更状態を確認する
変更されたファイルや新しく追加されたファイルを確認します。
```bash
git status
```
* **確認ポイント**: 変更されたファイルが赤字で表示されていること、および現在のブランチが `main` になっていることを確認します。

### 2. 変更したファイルをコミット対象に追加する
変更したすべてのファイルをコミットの準備状態（ステージ）に上げます。
```bash
git add .
```

### 3. コミット対象に入ったか確認する
正しくファイルが追加されたか再度確認します。
```bash
git status
```
* **確認ポイント**: 変更されたファイル名が **緑字**（`Changes to be committed:`）に変わっていることを確認します。

### 4. 変更内容を記録（コミット）する
メッセージ付きでコミットを実行します。
```bash
git commit -m "修正内容のわかりやすいメッセージ"
```
* **確認ポイント**: メッセージは `""`（ダブルクォーテーション）で囲んで記述します。

### 5. GitHubにプッシュ（アップロード）する
GitHubの `main` ブランチに変更を送信します。
```bash
git push origin main
```
* **確認ポイント**: エラーなく送信完了すれば、Cloudflare Pages が自動で検知し、本番環境へ数分で反映されます。
